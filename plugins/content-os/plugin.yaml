name: content-os
version: 2.4.0
description: "AI-Augmented Content Production System: signal → idea → brief → draft → verify → post → feedback. Full Content OS implementation with 4-route Idea Gate, 14-state lifecycle, 54 slop patterns, Writer/Verifier dual-agent pipeline."
author: "Adapted from Shann³ (@shannholmberg)"
kind: standalone
hooks:
  - on_session_start
  - post_tool_call
provides_tools:
  - content_os_manager
  - content_os_retriever
provides_hooks:
  - on_session_start
  - post_tool_call
